
import { LongPressDirective } from './long-press.directive';

describe('FullPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new LongPressDirective();
    expect(directive).toBeTruthy();
  });
});
